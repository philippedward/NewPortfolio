<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>{{ titre }}</title>
  </head>
  <body>
    <h1>Bonjour, {{ nom }} !</h1>

    {% if message %}
    <p>{{ message }}</p>
    {% endif %}
  </body>
</html>
