{# ============================================================
   carte-projet.njk — Carte résumée d'un projet
   Inclus dans index.njk via {% include "carte-projet.njk" %}
   à l'intérieur d'un {% for projet in projets %}.
   La variable "projet" est l'objet complet du fichier JSON.
   ============================================================ #}
